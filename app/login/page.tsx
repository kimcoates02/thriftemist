"use client";
import { supabaseBrowser } from "@/lib/supabase-browser"; import { useState } from "react"; import { useRouter } from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();const {error}=await supabaseBrowser().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push("/account")}return <div className="container max-w-md py-20"><h1 className="serif text-5xl">Login</h1><form onSubmit={go} className="mt-8 grid gap-4"><input type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="border border-black/15 bg-transparent px-4 py-3"/><input type="password" required placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="border border-black/15 bg-transparent px-4 py-3"/><button className="bg-[#153726] px-6 py-4 text-xs uppercase tracking-widest text-[#F8F3E0]">Login</button>{error&&<p className="text-sm text-red-700">{error}</p>}</form></div>}