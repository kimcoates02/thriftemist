import { supabaseServer } from "@/lib/supabase-server"; import { money } from "@/lib/format";
export default async function Orders(){const s=await supabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return <div className="container py-20">Please log in.</div>;const {data}=await s.from("orders").select("*").eq("user_id",user.id).order("created_at",{ascending:false});return <div className="container py-16"><h1 className="serif text-6xl">My Orders</h1><div className="mt-10 divide-y divide-black/10">{(data||[]).map(o=><div key={o.id} className="flex justify-between py-6"><div><div>#{o.order_number}</div><div className="mt-1 text-xs uppercase tracking-widest text-black/45">{o.status} · {o.payment_status}</div></div><b>{money(o.total)}</b></div>)}</div></div>}