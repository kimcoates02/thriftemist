import { ProductCard } from "@/components/product-card"; import { getProducts } from "@/lib/data";
export default async function Sold(){const p=await getProducts({status:"sold"});return <div className="container py-16"><p className="text-xs uppercase tracking-[.25em] text-[#B39A6B]">Archive</p><h1 className="serif mt-2 text-6xl">Sold Items</h1><p className="mt-5 max-w-xl text-sm leading-7 text-black/60">Pieces that found a home. Browse the archive and request a similar find.</p><div className="mt-12 grid grid-cols-2 gap-x-4 gap-y-12 md:grid-cols-4">{p.map(x=><ProductCard key={x.id} product={x}/>)}</div></div>}